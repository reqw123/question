# OpenAI API key 存在應用程式內建設定畫面，不用 .env

`host-app`／`desktop-pet` 現有的 Electron 模組都沒有任何設定檔/金鑰管理機制，`.env`（啟動時讀取、被 `.gitignore` 排除）原本是最省事的預設做法，也是純自用場景的業界慣例。但決定改成應用程式內建一個設定畫面讓使用者貼 key 進去、存到本機設定檔，不用 `.env`。之所以特別記錄：這跟專案其他模組一貫的「純檔案/環境變數」風格不同，未來讀到這段程式碼若疑惑「怎麼多一個設定 UI」，是刻意選擇，不是遺留代碼。
