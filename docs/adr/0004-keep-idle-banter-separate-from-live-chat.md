# 閒置閒聊維持寫死台詞，跟即時對話是兩套獨立機制

新增「即時對話」（真 LLM）後，考慮過乾脆把 `idle-chat.json` 的寫死閒聊也改成 AI 即時生成，讓角色全部對話都由 AI 產生。決定不合併：待機時角色會不定時自動觸發閒聊泡泡，如果每次都呼叫 OpenAI API 會在使用者沒有主動互動的情況下持續燒錢。維持「閒置閒聊＝免費寫死台詞、即時對話＝使用者主動觸發才呼叫 API」的分工，兩套機制故意分開（術語見 CONTEXT.md）。
