# question

個人專案集合：Live2D 桌面互動（桌寵助理、多人搶答遊戲）與周邊工具。這份文件只收錄容易混淆、需要統一用詞的領域概念，不是每個子專案的完整說明（各子專案的操作/架構細節見各自的 README/說明文件）。

## Language

### 桌面助理（desktop-pet）AI 對話相關

**閒置閒聊（Idle Banter）**：
角色待機時，從 `idle-chat.json` 隨機挑一組**寫死台詞**顯示成對話泡泡，不呼叫任何 AI 服務、不花費 API 額度。
_Avoid_: 聊天、AI 聊天（容易跟「即時對話」搞混）

**即時對話（Live Chat）**：
使用者主動點擊角色開啟輸入框，即時呼叫 OpenAI 取得回覆的對話功能。跟「閒置閒聊」是兩個獨立機制，故意不合併（見 ADR-0004）。
_Avoid_: 聊天（太籠統）、AI 助理

**角色人設（Character Persona）**：
桌寵每個 Live2D 角色（角色一／角色二）在「即時對話」中各自獨立的 system prompt 與對話記憶。點擊哪個角色，就套用該角色的人設與記憶，兩者互不相通。

**網頁摘要（Page Digest）**：
使用者在「即時對話」中貼上網址，程式自動偵測並在後端抓取該網址內容，交給 AI 摘要／回答。純唯讀，不做瀏覽器自動操作、不模擬點擊。
_Avoid_: Browser Use（原始需求用詞範圍太廣，容易誤解成 AI agent 直接操控瀏覽器介面，見 ADR-0005）

**語音輸入（Voice Input）**：
「即時對話」輸入框裡的 🎤 功能。點一下開始錄音，偵測到使用者停止說話（或再點一次手動停止、或 60 秒上限）就結束錄音，呼叫 OpenAI Whisper API 轉錄、用打字機效果顯示在輸入框、直接送出。**單次錄音，每次都要手動點 🎤 開啟**——講完這一句、轉錄送出之後就回到待命狀態，要講下一句得再點一次（曾經試過送完自動接著錄下一段的「語音模式」，會有聲學回授/幻覺輸入的問題，改回每次手動開啟）。跟「語音輸出」是同一個「語音」需求底下的獨立子功能，各自獨立的 ADR（見 ADR-0002、ADR-0007）。
_Avoid_: 語音辨識（跟「語音輸入」是同一件事，統一用「語音輸入」）；語音模式（已改回單次錄音，不再有這個持續狀態，不要用這個詞）

### desktop-pet-web（介紹網站）互動功能

**本機控制面板（Local Control Panel）**：
desktop-pet-web 頁面上遙控「真正在使用者電腦上跑的桌寵」（desktop-pet Electron App）的區塊。透過 `control-server.js`（`127.0.0.1:47821`，只接受 loopback 來源）呼叫 `main.js` 既有函式（show/hide/toggleInteractive/randomMotion/resetPosition）。**只有這個機制會影響真正的桌寵**——跟下面的「手勢拖曳」是完全不同的兩件事，不要混用「操控桌寵」這個籠統說法去指稱兩者。

**手勢拖曳（Gesture Drag）**：
desktop-pet-web 首頁 Hero 區塊 Live2DCard 卡片上的手勢互動：食指指尖觸碰卡片內模型輪廓、停留 2 秒後選定該模型，之後模型位置跟隨手部移動；手勢變成 Closed_Fist（握拳，石頭）即取消選定，模型停在當前位置。**純前端、頁面本地效果，移動範圍限制在卡片畫布邊界內，完全不會呼叫「本機控制面板」那套 API，也不會影響使用者電腦上真正在跑的桌寵**——這是刻意的架構決定，見 ADR-0011、`docs/specs/0010-desktop-pet-web-gesture-drag.md`。
_Avoid_: 手勢控制、手勢操控桌寵（容易誤會成會影響真正的桌寵，見上面「本機控制面板」的區分）

**手勢動作觸發（Gesture Action Trigger）**：
desktop-pet-web 高精度 3D 模型展示卡片（Aatrox3DShowcase）裡的手勢功能：比出 👍（Thumb_Up）即觸發下拉選單目前選定的動作，等同「鏡頭版的空白鍵」。跟「操控模式」（WASD＋空白鍵）互斥，一次只能開一種輸入方式。**純前端、頁面本地效果，完全不會呼叫「本機控制面板」那套 API，也不會影響使用者電腦上真正在跑的桌寵**——跟「手勢拖曳」（移動模型位置）是兩個各自獨立的手勢功能，即使桌寵沒有在跑也照常可用。見 ADR-0013、`docs/specs/0012-desktop-pet-web-gesture-action-trigger.md`。
_Avoid_: 手勢控制、手勢操控桌寵（同上）；跟「手勢拖曳」混用（兩者是不同卡片上的不同機制，一個是移動模型位置、一個是觸發動作，不要當成同一件事描述）

**手勢 PK 對戰模式（Gesture PK Mode）**：
desktop-pet-web 的雙人區網對戰功能：Aatrox3DShowcase 卡片的按鈕進入全螢幕擂台畫面，兩位玩家各自用手機比手勢操控攻擊／防禦／跳躍，透過 MQTT 區網連線對戰。跟「手勢動作觸發」「手勢拖曳」是完全獨立的第三種手勢機制，牽涉的範圍（全螢幕、雙人連線、跨裝置）也大得多，不要跟前兩者混用「手勢功能」這種籠統說法一起描述。見 ADR-0014、`docs/specs/0013-desktop-pet-web-gesture-pk-mode.md`。

**擂台端（Arena）／操控端（Controller）**：
「手勢 PK 對戰模式」裡的兩種角色分工。擂台端是實際渲染兩個 3D 角色、做命中判定、顯示血量的那台裝置（通常是桌機瀏覽器，desktop-pet-web 網址不帶參數）；操控端是另一位玩家的手機（desktop-pet-web 網址、路徑換成 `/pk`，2026-08-18 從 `?role=controller` query string 改成短路徑，見 `docs/specs/0013` 實作備註），只負責鏡頭手勢辨識，**完全不做 3D 渲染**，把手勢變成事件送給擂台端。**擂台端操作者本人同時就是玩家一**（2026-08-18 修訂，見 `docs/adr/0014` 修訂記錄）——擂台端不是中立的第三方顯示器，自己也要開鏡頭比手勢；操控端永遠是玩家二，只要有一個人連上就自動開始，不需要湊到兩支手機。跟 `multi/` 系統的「主持人／玩家」是類比但不是同一套機制，兩邊的 MQTT topic 命名空間（`pk/*` vs `quiz/multi/*`）完全分開，不要混用彼此的術語。
_Avoid_: 主持人／玩家（那是 `multi/` 系統的用詞，這裡沒有「主持」的概念，擂台端本身也是其中一位玩家）；「擂台端只負責顯示、不算玩家」（2026-08-18 之前的舊設計，已經不成立，見上）
