// 桌寵展示頁的可調整參數集中放這裡，方便日後不用進元件程式碼找數字改。

// 手勢拖曳同時最多能讓幾個角色「逃出」卡片、飄在 Hero 區塊——見
// docs/specs/0010-desktop-pet-web-gesture-drag.md「單一共用畫布上線後的四項回報修復」。
// 這個上限原本是為了避免多個 WebGL context 同時存在的風險（每個模型各自一個
// PIXI.Application 的舊架構），改成單一共用畫布之後這個顧慮已經不存在，現在純粹是
// UX 考量：畫面上同時飄太多角色會不會太亂，以及要留至少一個角色「釘」在卡片裡（逃出
// 沒有自動歸位機制，見 HeroLive2DStage.tsx）。
// 2026-08-12：角色數調整過幾次（曾經一度到 7 個），最後精簡回 3 個（077、1024100、
// Asuna）。這個上限維持原本的保守值 2，不跟著角色數等比例放大——「同時飄太多角色會
// 不會太亂」是看畫面上絕對數量,不是看總角色數的比例,3 選 2 已經足夠展示手勢拖曳這個功能,
// 不需要更多。只要角色數 > 2 就一定會留角色在卡片裡,這個約束目前還滿足。
export const MAX_ESCAPED_MODELS = 2
